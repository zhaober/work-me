## v1.7.0 课表模块 + 桌面小组件

### 新增功能

**课表模块**
- **课表页面**：新增第 5 个底部 Tab「课表」，类似 WakeUp 课程表的周视图
- **学期系统**：多学期管理，开学日期、总周数配置，当前周自动计算
- **课程 CRUD**：手动添加/编辑/删除课程（名称、教室、老师、星期、节次、颜色、周次、单双周、备注）
- **Excel 导入**：自动识别学校课表 Excel，解析上课时间、上课周、任课老师字段
- **节次配置**：5 个可调整的节次块（第1-2节 08:00-09:40 → 第9-11节 19:00-21:40）
- **当前时间指示**：今天列高亮 + 当前节次红点指示
- **周次系统**：单双周标记、多段周次（如 2-5,7-9,11-17）、当前周自动高亮
- **整周视图**：周一到周日 7 列（含周六周日），按 WakeUp 的比例排布

**桌面小组件（三款，同一套深色卡片视觉）**
- **课表网格**（4×3，可缩放）：整周课程表 —— 星期表头 + 左侧时间列 + 圆角彩色课程块，今天的日期高亮
- **今日课程**（2×2）：今天的课程、时间与教室
- **近日课程**（4×2）：今天起三天的课程安排
- **一键添加到桌面**：课表页「添加到桌面」→ 面板里实时预览三款小组件，点一下即可放置，不用去小部件列表里翻找
- **桌面上可被搜索到**：三款小组件都带独立名称与描述关键词

### 主要修复
- **小组件不显示课表**：插件注册时序错误 —— `registerPlugin` 必须早于 `super.onCreate`
- **桌面搜不到小组件**：receiver 缺少 `android:label`，补上并写全关键词描述
- **Excel 导入地点错成"多媒体"**：教务表里「教室类别」列抢占了「上课地点」，改为按优先级挑列
- **点「添加到桌面」没反应**：小米/红米调用 `requestPinAppWidget()` 本就不弹确认框，且需「桌面快捷方式」权限，否则静默失败 —— 改为直接尝试调用 + 失败原因分类回传 + 用小组件实例数前后对比给出确定结论
- **桌面报「载入窗口小部件时出现问题」**：布局里用了 RemoteViews 不认的 `<View>`（`android.view.View` 没有 `@RemoteView` 注解，会让整块布局 inflate 失败），改用 `TextView` 实现色块

### 技术亮点
- `schedule-core.js` 纯函数层：课表逻辑 + 小组件载荷（今日/近日/整周网格）全部可单测
- 圆角色块方案：JS 只下发颜色下标，原生映射到预置 drawable，规避 RemoteViews 无法动态上色的问题
- 载荷经 SharedPreferences 持久化，三款小组件各自独立同步、互不清空
- 三处镜像（源码 / www / APK assets）逐字节一致，有断言守护
- 小组件布局有静态守护测试：元素白名单（禁 `<View>`）、重复 id、drawable 引用、`android:angle` 合法性
- 全量单元测试 **621/621** 通过

### 版本信息
- versionCode 13
- versionName 1.7.0
- 提交: ad7b817
