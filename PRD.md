# 工作计划与复盘备忘录 App · 产品需求文档（PRD）

> 版本：v1.5.6（本地版 · v1.5.5 反馈修复）
> 最近更新：2026-08-30
> 状态：已实现并交付 Android Debug APK。v1.5.0 修复 4 项真机反馈（清除背景、实时时间、背景图文字可读性、退出后无提醒）；v1.5.1 消除白屏三类主因（移除 Google Fonts、崩溃捕获与看门狗、存储引导超时兜底）；**v1.5.3 经真机 logcat 锁定并修复闪退真正根因**——`LocalNotifications.cancel({})` 空载荷触发 Java 层 NPE，并补 v1.5.4 插件调用看门狗使同类原生崩溃可在面板中现形；**v1.5.5 修复「后台不弹通知」并放开文字颜色自定义**；**v1.5.6 修复用户真机反馈**：保存时偶发 `IDBObjectStore` key path 非法（由历史脏记录 id=null/undefined 导致），新增 `repairRecordId` 在落盘前自检修复；编辑器「设置提醒 + 日期」行增加卡片容器与背景区分。全量单元测试 352 例通过
> 平台：移动端 Android（设计基准 360×800，需自适应主流机型）

---

## 1. 产品概述

### 1.1 背景与问题
职场人每天既要规划工作（计划），也要定期总结（复盘）。现有备忘录/笔记工具多为扁平列表，难以把"计划"和"复盘"两类内容按项目或主题归类，也难以在复盘时直接对照原始计划。用户期望一种贴合"文件管理"心智的记录方式：像电脑目录一样层层归档，再在文件夹里沉淀计划与复盘；同时希望 App 具备个性化能力（主题、音效、风格、自定义背景），贴合自身审美与习惯。

### 1.2 产品定位
一款以 **多级文件夹分区 + 计划/复盘双类型** 为核心心智模型、采用 **块编辑器**、主打 **活力高效** 视觉，并支持 **个性化（主题 / 音效 / 风格 / 背景）** 的移动端备忘录 App。让每日计划与复盘结构化沉淀，并能在时间维度（日历/统计）上回顾，且不背单词式的轻量沉浸体验。

### 1.3 目标用户
- 职场白领、项目经理、运营/产品/研发同学；
- 有"日计划 + 日复盘"习惯、需要按项目/主题归档的人群；
- 偏好轻量、本地可用、不依赖账号、且重视个性化观感的工具型用户。

### 1.4 核心价值
- **双类型分色**：计划（靛蓝）/复盘（橙）徽标区分，状态一目了然；
- **文件式归档**：多级文件夹天然贴合"按项目归类"的心智；
- **计划对照**：复盘可关联计划，自动展示完成度对照卡；
- **灵活记录 + 时间回顾**：块编辑器自由书写，日历/统计提供回顾视角；
- **个性化表达（v1.2 新增）**：浅/深主题、交互音效、多套 UI 风格、自定义背景，体验更"像自己的"。

---

## 2. 范围与边界（本版本）

> **运行模式决策（已确认）**：本版本为 **本地运行** 的单机 App，不含云端账号体系，因此"云同步/多设备"功能在本版本 **暂缓**（见 §4 与 §11）。
>
> **v1.2 边界变更（本次反转旧决策）**：原 v1.1 曾"删除「我的」个人中心、底部导航仅保留 3 个 Tab"。根据用户最新需求，**重新引入「我的」个性化中心**（作为第 4 个底部 Tab + 侧边栏入口），并采用 **「侧边栏为主、底部 Tab 为辅」** 的导航形态；主题、音效、UI 风格、自定义背景均纳入本版本。

- **平台**：移动端 Android，最低兼容 Android 9+，主流机型与分辨率自适应（以 360×800 为基准）；
- **数据**：本地存储（**v1.4 起为 IndexedDB**，见 §7），不依赖服务端；
- **不在本版范围**：云端实时同步、多端协同、账号体系、团队协作、AI 自动生成内容；
- **已重新纳入**：「我的」个人中心（原 v1.1 删除，v1.2 恢复）；侧边栏导航（原无，v1.2 新增）；
- **已删除/暂缓模块**：云同步/多设备（同 F-13）。

---

## 3. 信息架构与核心概念

### 3.1 核心对象
- **文件夹（Folder）**：可 **无限层级** 嵌套（已确认不限制深度），类似电脑目录，例如 `工作 / 2026 Q3 / 新品上线`。
- **计划（Plan）**：一条计划记录，含标题、提醒、正文（块）、标签、附件、**优先级（高/中/低）**。
- **复盘（Review）**：一条复盘记录，可关联一条计划做对照；含不足说明、改进项、标签。
- **记录（Record）**：Plan 与 Review 的统称，出现在文件夹的"全部记录"列表中。
- **标签（Tag）**：用于分类与统计聚合，支持点击标签删除。
- **重要日子（Event）**：用户自定义的重要日期（亲友生日/纪念日等），周年重复，在日历以专属标记呈现（见 §4 F-12 衍生）。
- **我的 / 设置（Me）**：个性化中心，承载主题、音效、背景、重要日子管理、导出等（v1.2 新增）。
- **主题与风格（Theme/Style）**：浅色/深色主题，以及视觉风格（默认 / 液态玻璃 / 极简 / 景深），全局由 CSS 变量驱动（v1.2 新增）。

### 3.2 计划与复盘的关系（混合模型 · 已确认）
- 两类记录彼此 **独立**，可随时新建，不强制配对；
- 复盘可 **关联一条计划**，关联后展示"计划对照卡"（计划完成度进度条 + 计划要点），便于复盘时回顾目标；
- **v1.2 新增优先级**：计划/复盘可标记优先级（高/中/低），用于在文件夹内快速识别与"优先级归纳"查看。

### 3.3 信息架构树
```
侧边栏（主导航，左抽屉）
├─ 首页（文件夹总览）
│   ├─ 文件夹详情
│   │   ├─ 面包屑（可跳祖先级）
│   │   ├─ 子文件夹（可继续进入 / 可新建 / 可重命名 / 可删除 / 可选择性删除 / 可一键清空）
│   │   └─ 全部记录（计划·复盘混排，分色徽标；＋计划 / ＋复盘 两个新建入口）
│   │       └─ 优先级归纳视图（按 高/中/低 分组）
│   ├─ 计划编辑器（含删除按钮、优先级选择器）
│   └─ 复盘编辑器（含删除按钮、关联计划）
├─ 日历视图（年月可切换；含二十四节气 / 节日 / 自定义重要日子标记）
├─ 统计看板
└─ 我的（个性化中心：主题·音效·背景·重要日子·导出）

底部导航（辅助）：文件夹 · 日历 · 统计 · 我的
```

### 3.4 导航与交互约定
- 导航栈为对象结构 `{screen, id, recId, folderId}`，支持返回栈正确回退到来源屏（文件夹/搜索/日历/统计/我的）；
- **v1.2 导航变更**：以 **侧边栏（左抽屉）为主导航**，覆盖首页/日历/统计/我的及文件夹快捷入口；底部 Tab 作为辅助常驻入口（4 项）。两者均跳转同一路由，状态同步。
- 所有交互（新建/重命名文件夹、标签、提醒、年月跳转、关联计划选择、文件夹菜单、主题/风格选择、背景上传）均使用 **与应用风格一致的自定义弹窗（Modal）与底部 Action Sheet**，**不使用系统原生 prompt/confirm**。
- **v1.2 音效**：关键操作（页面切换、弹窗/底部菜单、保存、删除、勾选、添加清单项）播放 Web Audio 合成音效，无需音频文件；「我的」内可开关。

---

## 4. 功能需求清单（已落地状态）

| 编号 | 功能 | 说明 | 优先级 | 状态 |
|------|------|------|--------|------|
| F-01 | 多级文件夹管理 | 文件夹可无限层级嵌套，支持新建（首页与子文件夹内均可新建） | P0 | ✅ 已实现（无限层级） |
| F-02 | 计划记录 | 标题 + 提醒 + 块正文 + 标签 + 附件 + 优先级 | P0 | ✅ 已实现 |
| F-03 | 复盘记录（可关联计划） | 含关联计划对照卡、不足说明、改进项 | P0 | ✅ 已实现 |
| F-04 | 块编辑器 | 标题 / 自由正文 / 清单 / 图片 / 分割线 / 链接 | P0 | ✅ 已实现 |
| F-05 | 搜索 | 顶部搜索入口，按标题/标签/正文/文件夹名实时检索 | P1 | ✅ 已实现 |
| F-06 | 提醒 | 计划可设 HH:MM 提醒；编辑器含提醒入口 | P1 | ✅ 已实现（已升级为系统通知 + 震动 + 音效，支持多级提醒，见 F-23 / §11） |
| F-07 | 日历视图 | 月历网格，日期高亮，选中日展示当日记录；年月可切换 | P0 | ✅ 已实现 |
| F-08 | 标签管理 | 打标 + 标签聚合（统计页标签分布）+ 点击标签删除 | P1 | ✅ 已实现 |
| F-09 | 附件 / 图片 | 块编辑器支持插入图片块（插入即压缩），点击查看大图（Lightbox，支持放大/缩小/平移，见 F-24） | P1 | ✅ 已实现（图片入库自动压缩，见 F-25 / §7） |
| F-10 | 统计趋势 | 完成率、复盘篇数、连续天数、7 日趋势、标签分布（均由数据动态计算） | P0 | ✅ 已实现 |
| F-11 | 导出 | 导出为 JSON 文件（含 folders + records + events），本地保存；导出后明确告知文件位置，支持自定义导出目录 | P2 | ✅ 已实现（位置提示见 F-19；自定义目录见 F-20） |
| F-12 | 新建 / 重命名 / 删除 / 选择性删除 / 一键清空 | 文件夹"..."菜单：重命名、删除（递归）、选择性删除、一键清空；记录行侧选择 | P0 | ✅ 已实现 |
| F-13 | 云同步 / 多设备 | 账号 + 多端实时同步 | P2 | ⏸ 暂缓（本地版不含） |
| **F-14** | **交互音效** | 关键操作播放 Web Audio 合成音效（tap/pop/toggle/success/delete/warn），无需音频文件，「我的」内可开关 | P1 | ✅ 已实现（Web Audio 合成，无音频依赖） |
| **F-15** | **计划优先级** | 计划/复盘支持优先级（高/中/低），编辑器分段选择器；文件夹详情显示优先级彩条与徽标；提供「优先级归纳」按钮按高中低分组查看 | P1 | ✅ 已实现 |
| **F-16** | **侧边栏导航** | 左侧抽屉式主导航（含文件夹快捷入口、日历/统计/我的）；底部 Tab 保留为辅助（4 项） | P1 | ✅ 已实现（侧边栏 + 底部 Tab 并存，状态同步） |
| **F-17** | **个性化主题与「我的」** | 重新引入「我的」个人中心（第 4 Tab + 侧边栏入口）：浅色/深色主题切换、交互音效开关、自定义背景上传、自定义重要日子管理、导出、自定义导出目录、存储用量与一键优化 | P0 | ✅ 已实现（「我的」页 / 浅深主题 / 背景 / 导出目录 / 优化存储均已落地） |
| **F-18** | **UI 风格与自定义背景** | 多套视觉风格（默认 / 液态玻璃 / 极简 / 景深）可切换；界面背景支持用户自定义上传图片（属「我的」，已支持压缩与准确错误提示） | P1 | 🔧 部分实现（自定义背景 ✅ 已落地并压缩；UI 风格多套切换 📋 规划中） |
| **F-19** | **导出位置提示** | 导出成功后以弹窗明确告知文件保存位置（默认「系统下载目录」，自定义目录时显示具体路径） | P2 | ✅ 已实现（v1.3） |
| **F-20** | **自定义导出目录** | 用户可选择导出目标文件夹，目录句柄持久化于 IndexedDB，下次导出免重选；权限失效自动降级为下载 | P2 | ✅ 已实现（v1.3，File System Access API） |
| **F-21** | **滚盘选择器** | 重要日子（年/月/日）、记录日期、提醒时间改用滚盘（Wheel Picker）选择，替代手动输入，自动处理闰年/月天数 | P1 | ✅ 已实现（v1.3） |
| **F-22** | **背景压缩与存储错误提示** | 自定义背景上传即等比压缩（最长边 1280 / JPEG 0.72）再入库；区分「本地存储配额已满」与「手机存储空间不足」，错误提示准确 | P1 | ✅ 已实现（v1.3，见 §7 / §11） |
| **F-23** | **多级提醒闹钟** | 12306 式多级提醒：到点前提前 3h 起、每 30min 一次、最晚提前 5min，到点提醒；用系统通知 + 震动 + 音效 + 应用内提示触发；启动/保存后自动重排 | P1 | ✅ 已实现（v1.3，仍受 Web 限制，见 §11） |
| **F-24** | **图片查看缩放** | Lightbox 支持放大/缩小/平移：桌面滚轮、双击 1x↔2x、移动端双指捏合缩放 + 单指拖动平移，带倍率读数，开关自动复位 | P1 | ✅ 已实现（v1.3） |
| **F-25** | **存储占用优化** | 记录内图片插入即压缩（最长边 1600 / JPEG 0.82）；「我的」展示图片已占空间并提供「一键优化存储」对超限大图重压缩释放 | P1 | ✅ 已实现（v1.3；v1.4 起由 F-26~F-30 的新架构取代，见 §7） |
| **F-26** | **IndexedDB 存储内核** | 业务数据全部迁至 IndexedDB；正文经 lz-string 压成 `Uint8Array` 存 `content_compressed`，小字段（标签）走 `TextEncoder` 存 `style_data` | P0 | ✅ 已实现（v1.4） |
| **F-27** | **正文与元数据分表** | `notes` 存可索引元数据，`note_contents` 单独存压缩正文。IndexedDB 游标一次取整行，只有分表才能真正做到「列表查询不加载正文」 | P0 | ✅ 已实现（v1.4） |
| **F-28** | **图片 WebP Blob 直存 + 去重** | 图片不再转 Base64（体积 +33% 且占内存），改为 WebP Blob 直存 `note_images`：大图长边 1080 / q0.75，缩略图 200×200 / q0.65，入库前按 SHA-256 全局去重，重复图复用旧 id | P0 | ✅ 已实现（v1.4） |
| **F-29** | **防抖落盘与强制保存** | 输入停止 3000ms 写盘（每次输入重置定时器）；切后台 / 关页面（`visibilitychange` + `pagehide` + `beforeunload`）立即强制落盘；编辑已有记录自动存草稿 | P0 | ✅ 已实现（v1.4） |
| **F-30** | **旧数据迁移** | localStorage 旧数据一次性迁入 IndexedDB，旧 Base64 图片转 Blob 走新管线（压缩 + 去重）；靠 meta 标记保证幂等，迁移后保留旧副本作回滚保险 | P0 | ✅ 已实现（v1.4） |
| **F-31** | **清除自定义背景** | 修复「我的 → 自定义背景 → 清除」点击无效：清除按钮位于上传行内，事件委托中 `data-bg-upload` 判断先命中而误判为上传，改为先判断 `data-bg-clear` | P1 | ✅ 已实现（v1.5.0） |
| **F-32** | **新建记录实时时间** | 新建计划/复盘的时间默认取当前 `HH:MM`（`getNowTime()`），不再固定 09:00 | P1 | ✅ 已实现（v1.5.0） |
| **F-33** | **字体大小与文字颜色** | 自定义背景下文字可读性问题：「我的」新增字体大小（小/标准/大/特大）与文字颜色（自动/白/黑）；CSS 以 `--app-font-scale` 驱动 `calc` 缩放关键文字；有背景图时自动加反向 `text-shadow` | P1 | ✅ 已实现（v1.5.0） |
| **F-34** | **后台系统级提醒** | 集成 `@capacitor/local-notifications`，保存/删除/启动时同步系统层通知，App 退出或进程被杀后仍可收到提醒；通知 id 由记录 id 哈希映射为 32 位正整数；插件不可用时优雅降级 | P0 | ✅ 已实现（v1.5.0） |
| **F-35** | **启动崩溃捕获与看门狗** | 经典脚本（早于 module）捕获 `window error`（捕获阶段，含资源加载失败）与 `unhandledrejection`，写入 `work-memo-crash-log`；启动 10s 未置 `__WM_BOOT_OK__` 则弹错误面板（含阶段/堆栈，可一键复制）；阶段标记 `html-parsed → module-loaded → store-loading → rendered` | P0 | ✅ 已实现（v1.5.1） |
| **F-36** | **零远程资源依赖** | 移除 Google Fonts（2 个 preconnect + 1 个外部样式表）。样式表是渲染阻塞资源，域名不可达时 WebView 会一直白屏，表现为「打不开软件」；改用系统字体栈 `SYSTEM_FONT_STACK` | P0 | ✅ 已实现（v1.5.1） |
| **F-37** | **存储引导超时兜底** | `bootstrapStore()` 加 `raceTimeout` 超时（6s）。IndexedDB 在个别设备上可能既不 resolve 也不 reject，此时 `.then/.catch` 都不执行、App 永远停在空白页；超时后降级 localStorage 并留档，`storeBootAborted` 阻止迟到结果覆盖已渲染数据 | P0 | ✅ 已实现（v1.5.1） |

### 4.1 删除能力（迭代新增，已确认）
- **删除记录**：编辑器标题栏右上角（保存按钮左侧）提供垃圾桶按钮，仅编辑已有记录时显示，点击即删除计划/复盘；
- **删除文件夹**：文件夹详情页右上角"..."菜单 → 删除，递归删除其所有子文件夹与内部记录；
- **选择性删除 / 一键清空**：文件夹"..."菜单提供「选择性删除」（进入选择模式，勾选子文件夹/记录批量删除）与「一键清空」（清空当前文件夹下所有内容，保留文件夹本身）；
- **删除标签**：标签支持点击标签本身删除。

### 4.2 日历增强（迭代新增，已确认）
- **二十四节气**：按真实天文公式逐日计算，日期格以绿色圆点标记，日面板列出当日节气；
- **重要节日**：公历节日（元旦/劳动节/国庆等）+ 农历节日（春节/元宵/端午/七夕/中秋/重阳/腊八/小年），橙色圆点标记；
- **自定义重要日子**：用户可在日面板「＋ 添加重要日子」登记亲友生日/纪念日（周年重复），紫色圆点标记，可删除；存入 `DB.events` 持久化。

---

## 5. 页面与交互设计（已落地）

### 5.1 首页（文件夹总览）
- **结构**：问候 + 日期 → 搜索栏 → 今日概览（计划/复盘两张卡，数据驱动）→ 文件夹列表 → 新建文件夹（底部入口）。
- **交互**：点击文件夹行进入对应文件夹详情；点击今日概览卡进入当日记录；侧边栏或底部 Tab 切换屏；每日首次启动弹名人名言（可点击问候重弹）。

### 5.2 文件夹详情
- **结构**：返回/标题/"..."更多页头 → 面包屑 → 子文件夹网格（含「+ 新建文件夹」虚线卡）→ 全部记录（计划 / 复盘 分色，含优先级徽标与彩条）。
- **交互**：
  - 子文件夹卡可继续进入下一级；「+ 新建文件夹」任意层级生效（按当前文件夹隔离）；
  - 文件夹右上角"..."菜单：重命名、选择性删除、一键清空、删除文件夹（递归）；
  - 记录区拆为「＋计划 / ＋复盘」两个新建入口；记录行进入对应编辑器；
  - **优先级归纳**（v1.2）：文件夹详情提供「优先级归纳」按钮，按 高/中/低 分组列出当前文件夹及后代中的所有优先级记录。

### 5.3 计划编辑器
- **结构**：返回 / 类型徽标 / 路径 → 标题 → 提醒（如 18:00）→ **优先级分段选择器（高/中/低，v1.2）** → 正文（contenteditable）→ 清单块（含勾选态）→ 图片块（点击查看大图）→ 分割线 → 标签 → 底部块工具栏（文字/清单/图片/分割线/链接）；标题栏右上角含保存（对勾）与删除（垃圾桶，仅编辑已有记录时显示）。
- **交互**：提醒可点击设置 HH:MM；优先级点击切换；图片按钮选择文件并预览，点击已有图片全屏查看/替换/删除；标签「+添加」；保存写回 DB。

### 5.4 复盘编辑器
- **结构**：橙色「复盘」徽标 → 标题 → 关联计划卡（可「+关联计划」/解除）→ 正文 → 计划对照卡（完成度进度条）→ 不足说明 → 改进项块 → 标签；标题栏同样含保存与删除按钮。
- **交互**：关联计划卡点击可重新选择/解除关联，自动渲染计划完成度对比卡。

### 5.5 日历视图
- **结构**：月份导航（标题显示如 `2026年8月`，可点击跳转到输入 YYYY-MM）→ 月历网格（今日高亮，含节气/节日/重要日子彩色圆点）→ 选中日面板（如"8月27日·周四，今日 2 条" + 当日节气/节日/重要日子列表 + 「＋ 添加重要日子」）→ 当日记录列表。
- **交互**：左右箭头翻月（边界自动跨年）；标题点击输入年份月份跳转；网格按真实月天数 + 首日星期偏移（周一首列）渲染；今日高亮仅在显示年月等于今天时生效；记录行进入编辑器；点击「＋ 添加重要日子」登记自定义日期。

### 5.6 统计看板
- **结构**：标题 + 周期切换 → 完成率卡 → 小指标（复盘篇数 / 连续复盘天数）→ 7 日趋势柱状图 → 标签分布卡。
- **交互**：所有指标由 DB 动态计算；标签分布可点击下钻到搜索。

### 5.7 侧边栏导航（v1.2 新增）
- **结构**：左抽屉，顶部用户信息/应用名；中部导航项（首页 / 日历 / 统计 / 我的）+ 文件夹快捷入口（常用文件夹）；底部（设置/关于）。
- **交互**：从首页/任意主屏左滑或点汉堡按钮唤出；点击导航项跳转对应屏并同步底部 Tab 选中态；点击遮罩或选中项关闭抽屉；抽屉内文件夹快捷入口点击直达对应文件夹详情。

### 5.8 我的（个性化中心，v1.2 新增）
- **结构**：个人信息头 → 设置分组：
  - **主题**：浅色 / 深色 单选（即时切换，全局 `[data-theme]`）；
  - **音效**：交互音效 开/关 开关；
  - **背景**：自定义背景（从相册上传图片，上传即压缩，作为首页/通用背景；可恢复默认）；
  - **重要日子**：查看/管理自定义重要日子列表；
  - **导出文件夹**：选择导出目标目录（F-20，句柄持久化，可一键恢复默认下载目录）；
  - **存储优化**：展示图片已占空间；提供「一键优化存储」对超大图重压缩释放配额（F-25）；
  - **UI 风格**（规划中，F-18）：默认 / 液态玻璃 / 极简 / 景深 选择；
  - **导出**：导出 JSON（导出后弹窗告知保存位置，F-19）。
- **交互**：所有设置即时生效并持久化（见 §7 `settings`）；UI 风格与背景为 F-18 规划项，本期先预留入口与数据结构。

---

## 6. 视觉与体验规范

### 6.1 配色 Token（浅色）
| 角色 | 色值 |
|------|------|
| 背景 | `#F4F5FA` |
| 卡片 | `#FFFFFF` |
| 描边 | `#ECECF1` |
| 主文字 | `#1A1D29` |
| 次文字 | `#70747E` |
| 弱文字 | `#A0A4AE` |
| 主题靛蓝（计划） | `#4F6EF7` |
| 复盘橙 | `#FF8A3D` |
| 完成绿 | `#2BB673` |
| 节气绿（标记） | `#2BB673` |
| 节日橙（标记） | `#FF8A3D` |
| 重要日子紫（标记） | `#8B5CF6` |
| 优先级-高（红） | `#F5564A` |
| 优先级-中（橙） | `#FF8A3D` |
| 优先级-低（蓝） | `#4F6EF7` |

### 6.2 圆角与间距
- 卡片圆角 14–16；计划/复盘徽标 6；文件夹图标 10；复选框 5；进度条 3；图片块 12；
- 底部 Tab 药丸外层 36，选中态 26（选中为实色 `#4F6EF7`）；
- 侧边栏宽度约 78% 屏宽，左抽屉圆角右侧 16；
- 内容容器内边距 20/20/16；模块间距 16–18。
- **安全区（迭代新增，已确认）**：不使用应用内假状态栏；采用 `viewport-fit=cover` + `env(safe-area-inset-*)` 适配刘海/状态栏/底部手势区；应用容器宽度 100%、最大 480px，大屏手机不留黑边。

### 6.3 字体
全局字体 `Inter`（中文回退 PingFang SC / 微软雅黑）。标题 18–20、正文 14–15、次要 12–13。

### 6.4 风格基调
活力高效：卡片化、轻投影、主题色点缀，类似 Todoist / 飞书的轻快观感；**v1.2 引入"不背单词"式的轻量沉浸气质**——大留白、弱分割、克制的动效与音效，降低记录时的认知负担。

### 6.5 主题模式（浅色 / 深色，v1.2）
- 全局以 CSS 变量驱动，根节点 `[data-theme="dark"]` 切换；
- 深色主题 token（草案）：背景 `#15171E`、卡片 `#1E212B`、描边 `#2A2E3A`、主文字 `#ECEEF5`、次文字 `#9AA0AE`、弱文字 `#6B7180`；计划靛蓝/复盘橙/完成绿保持辨识度并适当提亮；
- 切换即时生效，持久化于 `settings.theme`。

### 6.6 UI 风格（默认 / 液态玻璃 / 极简 / 景深，v1.2 规划 · F-18）
- **默认**：当前活力高效卡片风；
- **液态玻璃（Liquid Glass）**：半透明卡片 + `backdrop-filter: blur()` 毛玻璃、细高光描边，适合自定义背景上叠加；
- **极简（Minimal）**：去卡片描边与投影，纯文字分区，大留白；
- **景深（Depth）**：背景虚化 + 前景浮起投影，强化层级纵深。
- 风格切换影响全局容器/卡片/背景层样式，由 `settings.style` 控制。

### 6.7 交互音效（v1.2 · F-14）
- 预设：`tap`（轻点）、`pop`（弹窗）、`toggle`（开关/勾选）、`success`（保存成功）、`delete`（删除）、`warn`（警示）；
- 浏览器环境用 Web Audio API 实时合成（短促正弦/三角波包络），**无音频文件依赖**；Node/无音频环境静默降级；
- 全局 `playSound(name)` 封装，在点击/切换/保存/删除等关键节点调用；「我的」内 `soundOn` 关闭即不播放。

### 6.8 自定义背景（v1.2 · F-18 关联）
- 用户在「我的 → 背景」上传图片，以 dataURL 存入 `settings.bgImage`，作为首页/通用背景层；
- 与 UI 风格联动：液态玻璃/景深风格下背景更显著；可一键恢复默认。

---

## 7. 数据结构（本地）

> **v1.4 重大变更：存储从 localStorage 迁至 IndexedDB。** 详细设计见 §7.1。

启动时 `bootstrapStore()` 异步引导：优先从 IndexedDB 恢复，IndexedDB 不可用时自动降级回 localStorage（`STORAGE_MODE = 'idb' | 'ls'`）；无任何数据时回退内置种子。写库点 `saveDB()` 采用**防抖 3000ms** 落盘，切后台 / 关页面时 `flushPersist()` 立即强制落盘。

### 7.1 IndexedDB 表结构（v1.4）

库名 `work-memo-idb`，版本 1，共 4 个对象仓库：

```text
notes（主表，keyPath: id）
  { id, type, folderId, title, date, time, reminder, priority,
    image_id, update_time }
  索引：by_update(update_time) / by_folder(folderId) / by_date(date)
  —— 列表页只扫这张表，不含正文

note_contents（正文表，keyPath: id）
  { id, content_compressed: Uint8Array, style_data: Uint8Array }
  —— content_compressed = LZString.compressToUint8Array(JSON({body, checklist}))
  —— style_data         = TextEncoder(JSON({tags}))   // 小字段，不压缩
  —— 分表原因：IndexedDB 游标一次取整行，同表无法做到「列表不加载正文」

note_images（图片表，keyPath: id）
  { id, note_id, hash_sha, blob_full: Blob, blob_thumb: Blob,
    w, h, bytes, created }
  索引：by_hash(hash_sha, unique) / by_note(note_id)
  —— Blob 直存，不做 Base64（体积 +33% 且整块进内存）

meta（键值表，keyPath: k）
  { k, v }  —— folders / events / settings / migratedFrom
```

图片入库管线（`compressImage`）：
1. `createImageBitmap` 解码（不可用回落 `Image`）；
2. 大图：长边缩放到 1080，`canvas.toBlob('image/webp', 0.75)`；
3. 缩略图：200×200 居中裁剪，`toBlob('image/webp', 0.65)`；
4. `crypto.subtle.digest('SHA-256')` 计算哈希 → 查 `by_hash` 唯一索引 → 命中则**复用旧 id、放弃本次 Blob 存储**；
5. WebP 不支持时回落 JPEG（避免浏览器静默降级成 PNG 反而更大）；`crypto.subtle` 不可用（非安全上下文）时回落 FNV 指纹，去重不中断。

运行时以内存中的 `DB` 为唯一渲染源；图片 Blob 渲染前转 `URL.createObjectURL`，带缓存与释放（`releaseUnusedImageUrls`）。

```text
Folder { id, name, parentId, color, createdAt }
Record {
  id, type: 'plan' | 'review',
  folderId, title,
  blocks: Block[], tags: string[],
  reminder?: string,        // 如 "18:00"
  linkedPlanId?: string,    // 仅 review 使用
  priority?: 0|1|2|3,       // v1.2：0 无 / 1 高 / 2 中 / 3 低
  createdAt, updatedAt
}
Block { type: 'text'|'checklist'|'image'|'divider'|'link',
        text?, checked?, src?, href? }
Tag { id, name, color }
Event { id, name, month, day, date?, color?, createdAt }   // v1.2 自定义重要日子
Settings {                                                // v1.2 个性化；v1.3 增强
  theme: 'light'|'dark',
  style: 'default'|'liquid-glass'|'minimal'|'depth',
  bgImage?: dataURL,                       // 自定义背景（已压缩入库）
  soundOn: bool,
  reminderPlan?: { startMin, intervalMin, stopMin, includeTarget }  // 多级提醒方案（默认见 F-23）
}
// 导出目录句柄持久化于 IndexedDB（非 settings），运行时恢复，避免重复授权
```

> **存储约束（v1.4 已根本性改善）**：
> - **配额**：v1.4 起业务数据存 IndexedDB，不再受 localStorage 约 5MB 配额限制。
> - **正文**：经 lz-string 压缩为 `Uint8Array`，重复中文实测压缩到约 5%，且列表查询不加载正文。
> - **图片**：WebP Blob 直存 + SHA-256 全局去重，同图反复插入只占一份空间；**严禁 `toDataURL` 产出存储用图**（`toDataURL` 仅用于 1×1 画布的 WebP 格式探测）。自定义背景仍为压缩后的 dataURL（`settings.bgImage`，最长边 1280 / JPEG 0.72）。
> - **写入**：防抖 3000ms + 切后台强制落盘；编辑已有记录自动存草稿。**新建记录不自动入库**，返回键 = 放弃未保存改动。
> - **删除**：删笔记先删关联图片再删记录；写盘时按差集清理内存中已删除的记录，避免幽灵数据；提供 `cleanupOrphanImages` 清理孤儿 Blob。
> - **空间释放（Web 局限）**：IndexedDB 删除记录后不会自动归还磁盘空间，浏览器按源整体管理，代码层面无法强制收缩；可在开发者工具中清除站点数据。因图片已压缩到最小，实际影响有限。
> - **降级**：IndexedDB 不可用时自动降级 localStorage，App 不白屏。

---

## 8. 非功能需求

- **性能**：冷启动 < 1s；列表滚动 60fps；统计计算 < 200ms；液态玻璃 `backdrop-filter` 在低端机需评估降级（见 §11）。
- **兼容**：Android 9+，主流机型与分辨率自适应（已通过 `safe-area-inset` 适配状态栏/手势区，基准 360×800）。
- **存储**：本地存储；**v1.4 起业务数据存 IndexedDB（正文压缩、图片 WebP Blob + 去重）**，不再受 localStorage 5MB 配额约束；单用户建议上限 200MB。
- **测试**：`npm test`（`node --test tests/*.test.js`）。除纯函数与源码断言外，另含 **12 例 IndexedDB 集成测试**（devDependency `fake-indexeddb`），未安装依赖时自动跳过、不阻塞。
- **可访问性**：文字与背景对比度满足 WCAG AA；交互元素点击区 ≥ 44px；深色主题需保证对比度。
- **一致性**：所有弹窗使用自定义 Modal / Action Sheet，禁止系统原生 prompt/confirm；音效风格统一，可全局关闭。

---

## 9. 技术实现与工程状态（已交付）

> 本节记录已确认的技术落地状态，供后续工程接管参考。

- **前端形态**：单文件 Web App（`work-memo-app.html`），数据驱动渲染（统一 `DB` 模型 + `renderHome/renderFolder/renderCalendar/renderStats/renderSearch/renderMe/loadEditor/paintEditor/saveEditor`）。
- **逻辑拆分**：纯函数抽到 `src/app-core.js`（日期/问候/名言/清单/选择删除/日历节气/优先级等），音效抽到 `src/sound.js`，侧边栏逻辑抽到 `src/sidebar.js`；均配 Node `node --test` 单元测试。
- **封装方式**：Capacitor v7 打包 Android，工程目录 `WorkMemoApp/`；`capacitor.config.json`（appId `com.workmemo.app`、appName `工作备忘录`、webDir `www`）。
- **构建环境（本机已就绪）**：
  - JDK 21（Temurin，路径 `C:/Users/33125/.workbuddy/tooling/jdk21/jdk-21.0.12.1+1`）；
  - Android SDK（`ANDROID_HOME=C:/Users/33125/.workbuddy/tooling/android-sdk`，含 `platforms/android-34`、`build-tools/34.0.0`、`platform-tools`，许可证已接受）；
  - Gradle 8.11.1（已预下载分发，绕开 wrapper 在线下载超时）。
- **构建命令**：
  ```bash
  cd WorkMemoApp/android
  export JAVA_HOME="C:/Users/33125/.workbuddy/tooling/jdk21/jdk-21.0.12.1+1"
  export ANDROID_HOME="C:/Users/33125/.workbuddy/tooling/android-sdk"
  ./gradlew.bat assembleDebug --no-daemon
  ```
- **已交付产物**：`工作备忘录-app-debug.apk`（debug 签名，约 4MB）。重新打包流程：更新 `work-memo-app.html` → 复制到 `WorkMemoApp/www/index.html`（并同步 `android/app/src/main/assets/public/index.html`）→ 重跑上述构建。
- **主题/风格实现要点（v1.2）**：主题与风格通过根节点 `data-theme` / `data-style` 属性 + CSS 变量切换；侧边栏为左抽屉 overlay 组件；均复用既有 `DB`/`settings` 持久化。
- **签名说明**：当前为 debug 签名，自用安装无碍；若上架应用商店需生成正式 release 签名（待办）。

---

## 10. 里程碑（更新）

| 里程碑 | 内容 | 状态 |
|--------|------|------|
| M1 | 设计稿 + 可交互原型（7 屏 → 现 6 屏） | ✅ 已完成 |
| M2 | 本地数据持久化（localStorage） | ✅ 已完成（v1.4 起迁移至 IndexedDB，见 M11） |
| M3 | 搜索 / 提醒 / 标签完整实现 | ✅ 已完成 |
| M4 | 导出能力（JSON） | ✅ 已完成 |
| M5 | 删除能力（记录 / 文件夹 / 选择性删除 / 一键清空）、重命名、文件夹"..."菜单 | ✅ 已完成 |
| M6 | Android 适配（安全区 / 自定义弹窗替代系统 prompt） | ✅ 已完成 |
| M7 | 封装 Android APK 并本机构建交付 | ✅ 已完成（debug） |
| M8 | 交互音效 + 计划优先级 + 日历节气/节日/重要日子 | ✅ 已完成 |
| M9 | 侧边栏导航 + 「我的」个人中心 + 浅/深主题 + 背景 + 导出目录 + 存储优化 | ✅ 已完成 |
| M10 | UI 风格（液态玻璃/极简/景深）多套切换 | 📋 规划中（F-18；自定义背景上传已落地，多风格切换待实现） |
| M11 | 存储架构重构：IndexedDB + 正文压缩 + 图片 WebP 去重 + 防抖落盘 + 旧数据迁移 | ✅ 已完成（F-26~F-30，v1.4） |
| M12 | 真机反馈修复：清除背景、新建记录实时时间、背景图文字可读性、退出后仍可提醒 | ✅ 已完成（F-31~F-34，v1.5.0） |
| M13 | 启动稳定性：崩溃捕获与看门狗、零远程资源依赖、存储引导超时兜底 | ✅ 已完成（F-35~F-37，v1.5.1） |

---

## 11. 开放问题与风险

- **提醒可靠性（v1.5.0 已接入原生通知）**：已集成 `@capacitor/local-notifications`（F-34），保存/删除/启动时同步系统层通知，App 退出或进程被杀后仍可收到提醒。剩余限制：
  - **国产 ROM 后台管控**：小米/华为/OPPO 等需用户手动开启「允许后台运行」「自启动」，否则系统会拦截 AlarmManager 唤醒；
  - **精确闹钟**：已声明 `SCHEDULE_EXACT_ALARM`，但 Android 12+ 用户仍可在系统设置中关闭，关闭后提醒可能延迟；
  - **插件版本差**：`@capacitor/local-notifications` 官方最新为 7.0.0，与 `@capacitor/core` 7.6.8 存在版本差（官方更新滞后），当前运行无异常。
- **启动稳定性（v1.5.4 已彻底修复）**：v1.5.1 消除「白屏/打不开」三类主因（远程字体渲染阻塞 F-36、存储层挂起无超时 F-37、崩溃无可见反馈 F-35）；**但 v1.5.1 的修复方向有误——真机 logcat 显示闪退真正根因是 `LocalNotifications.cancel({})` 空载荷触发 Java 层 NPE（v1.5.3 修复），而非 Web 层问题**。v1.5.4 进一步加插件调用看门狗：所有原生插件调用调用前留痕、成功后清除，若进程在 Java 线程被带崩，下次启动崩溃面板会点名嫌疑调用，使「沉默闪退」变为可诊断。
- **云同步是否纳入后续版本？** 当前本地版不含；若需多设备，需补充账号体系（F-13）。
- **存储容量（v1.4 已解除硬约束）**：业务数据迁至 IndexedDB 后不再受 localStorage ~5MB 配额限制，正文压缩 + 图片 WebP 去重进一步降低占用。剩余风险：
  - **空间不自动归还**：IndexedDB 删除记录后浏览器不会把磁盘空间还给系统（按源整体管理），代码层面无法强制收缩，需用户在开发者工具中清除站点数据。因图片已压至最小，影响有限。
  - **安全上下文依赖**：`crypto.subtle`（SHA-256 去重）仅在 https / localhost 下可用。Capacitor 容器内为 `https://localhost` 正常；若直接以 `file://` 打开 HTML，去重会回落 FNV 指纹（功能不中断但强度降低）。
  - **孤儿图片**：跨笔记去重复用图片时，删除持有者会留下 `note_id` 悬空的记录，由 `cleanupOrphanImages` 定期清理。
- **迁移风险**：localStorage → IndexedDB 为一次性迁移，迁移后保留 localStorage 副本作回滚保险（不主动删除）。旧版本 App 读到新数据不会受影响（新库独立）。
- **子文件夹嵌套深度**：已确认不限制，但极深路径需关注面包屑与性能。
- **导出格式**：当前仅 JSON；是否补充 Markdown/PDF 由产品确认。
- **正式签名**：上架需 release 签名，当前为 debug。
- **统计口径**："完成率"按勾选清单计算，已与用户确认口径；后续如需"按计划标记完成"可扩展。
- **v1.2 新增风险**：
  - **深色主题细节**：深色下各标记色/优先级色/对比度需逐屏校准（M9 细化中）；
  - **液态玻璃性能**：`backdrop-filter` 在低端 Android 机可能掉帧，需准备降级策略（如极简风格兜底）；
  - **侧边栏 vs 底部 Tab**：两者并存时路由状态需严格同步，避免选中态错乱；
  - **UI 风格切换成本**：多风格共用一套 DOM，样式需以 CSS 变量/属性隔离，避免相互污染（F-18 实现时重点验证）。

---

*本文档基于设计稿「工作计划与复盘备忘录App」、需求评审结论与 2026-08-27 至 08-30 的历次迭代（功能落地 / APK 修复 / v1.2 个性化增强 / v1.3 体验与稳定性修复 / v1.4 存储架构重构 / v1.5 真机反馈与启动稳定性修复）整理，作为后续开发依据。*

*v1.4 相对 v1.3 的关键变更：业务数据由 localStorage 迁至 IndexedDB（F-26）、正文与元数据分表以做到列表不加载正文（F-27）、图片改为 WebP Blob 直存并按 SHA-256 全局去重（F-28）、防抖 3000ms 与切后台强制落盘（F-29）、localStorage 旧数据一次性迁移（F-30）。全量单元测试 238 例通过（含 12 例 IndexedDB 集成测试）。*

*v1.5.0 相对 v1.4 的关键变更：修复「我的 → 自定义背景 → 清除」按钮无效（F-31）、新建记录时间改为实时当前时间（F-32）、新增字体大小与文字颜色设置以改善自定义背景下的文字可读性（F-33）、集成 Capacitor LocalNotifications 实现退出应用后的系统级提醒（F-34）。*

*v1.5.1 相对 v1.5.0 的关键变更（启动稳定性专项）：移除 Google Fonts 外部依赖以消除渲染阻塞导致的白屏（F-36）、新增启动崩溃捕获层与看门狗错误面板（F-35）、存储引导加 6 秒超时兜底以杜绝 IndexedDB 挂起导致的永久空白（F-37）。已用 Chrome DevTools Protocol 在真实计时下验证启动链路（stage=rendered、0 未捕获异常）。全量单元测试 296 例通过。*

*v1.5.3 相对 v1.5.1 的关键变更（闪退根因修复）：用户 logcat 显示 `FATAL EXCEPTION: CapacitorPlugins` + `JSArray.toList() on a null object reference`，定位为 `LocalNotifications.cancel({})` 空载荷——Capacitor `PluginCall.getArray` 字段缺失返回 null，插件未判空即 `toList()` 触发 NPE。修复：清空通知改为先 `getPending()` 取真实列表、经 `buildCancelPayload` 构造合法载荷、`shouldCallCancel` 守卫后才下发；通知 id 派生收敛到 `notificationIdFor`（值域 1..2147483646，规避 0 被视为未设置）；新增 5 个可测纯函数 + 19 例测试。全量 315 例通过。*

*v1.5.4 相对 v1.5.3 的关键变更（插件调用看门狗）：所有 `LocalNotifications` 调用经 `safeLN` 包装——调用前 `markPluginCall`、成功 resolve 后 `clearPluginCall`；若调用在 Java 线程把进程带崩，残留标记由经典脚本在下次启动时转成崩溃面板线索。这是「Java 层崩溃 JS 无法捕获」结构性风险的防御层。新增 14 例测试（含「HTML 不再有裸 `LN.cancel/schedule/...` 调用」静态断言）。全量 329 例通过。详见 `WorkMemoApp/CRASH_ANALYSIS_2026-08-30.md`。*

*v1.5.5 相对 v1.5.4 的关键变更（后台通知修复 + 文字颜色自定义）：**后台不弹通知**根因——Android 13+ 须显式声明 `POST_NOTIFICATIONS` 并在用户手势内请求，否则系统不弹窗；原调度路径在启动期无手势调用 `requestPermissions` 会被自动拒绝。修复：Manifest 显式声明 `POST_NOTIFICATIONS`；`scheduleRecordNotification` 调度前改 `checkPermissions` 校验而非请求；真正的 `requestPermissions` 收敛到保存/设提醒/设置页三处用户手势；注册 `appStateChange` 监听，切回前台时重登记原生通知防被系统清理；设置页新增「通知权限」实时状态与「发送测试通知」排查入口（MIUI 等系统可能默认把通知设为无弹窗，可用测试通知验证并引导去系统设置开启悬浮通知）。**文字颜色**：由 auto/白/黑 放开为支持任意 `#RRGGBB` 自定义色——新增 `isHexColor`/`normalizeTextColor`(接受十六进制)/`hexToRgb`/`relativeLuminance`/`customTextColorVars`/`textShadowForHex` 等纯函数；设置页改为「精选色板 chips + 取色器」组合；自定义色以亮度自适应阴影保证照片背景可读。新增 issue-42(自定义色)/issue-43(后台通知) 测试、修订 issue-41 断言；全量 345 例通过。*

*v1.5.6 相对 v1.5.5 的关键变更（真机反馈修复）：**保存失败**——用户截图报错 `Failed to execute 'put' on 'IDBObjectStore': Evaluating the object store's key path yielded a value that is not a valid key`。根因为 DB.records 中存在 id 为 null/undefined/非字符串的历史脏记录，导致 `putManyNotes` 整批失败。新增 `repairRecordId` 纯函数，在 `persistToIdb` 写入前自检修复：id 缺失/非法但对象键合法时补为对象键（保持 UI 稳定），无法修复则删除。**编辑器视觉分区**——用户反馈「设置提醒 / 日期」行与背景混为一体，为该区域新增 `editor-meta-card` 卡片容器（背景 + 边框 + 圆角），明确可点击输入。新增 issue-44 测试；全量 352 例通过。*
